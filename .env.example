# Frontend API path (no trailing slash) — proxied via next.config rewrites
NEXT_PUBLIC_API_URL=/api

# Backend server for Next.js rewrite proxy (no trailing slash)
API_PROXY_URL=http://localhost:5000
